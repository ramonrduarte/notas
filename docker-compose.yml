services:
  sefaz-downloader:
    build: .
    container_name: sefaz-downloader
    ports:
      - "${PORT:-8100}:8000"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - TZ=America/Sao_Paulo
    restart: unless-stopped
